# data/

Thư mục này chứa dữ liệu tĩnh dùng cho API, ví dụ:

- mapping tỉnh/thành theo `province_version` (legacy_64/current_34)

Bản đầu có thể dùng JSON; khi cần quản trị/cập nhật thường xuyên có thể chuyển sang DB.


