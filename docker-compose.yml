version: '3.8'

services:
  api:
    build: .
    container_name: cccd-api
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - FLASK_ENV=production
      - DEFAULT_PROVINCE_VERSION=current_34
      - API_KEY_MODE=simple
      # Uncomment và điền giá trị khi cần:
      # - API_KEY=your-api-key-here
      # - API_KEY_MODE=tiered
      # - MYSQL_HOST=mysql
      # - MYSQL_PORT=3306
      # - MYSQL_USER=root
      # - MYSQL_PASSWORD=your-password
      # - MYSQL_DATABASE=cccd_api
      # - ADMIN_SECRET=your-admin-secret
    volumes:
      # Mount logs directory (optional)
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import requests; requests.get('http://localhost:8000/health').raise_for_status()"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

  # Uncomment nếu dùng tiered mode với MySQL:
  # mysql:
  #   image: mysql:8.0
  #   container_name: cccd-mysql
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=your-root-password
  #     - MYSQL_DATABASE=cccd_api
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./scripts/db_schema.sql:/docker-entrypoint-initdb.d/01-schema.sql
  #   ports:
  #     - "3306:3306"
  #   restart: unless-stopped

# volumes:
#   mysql_data:
