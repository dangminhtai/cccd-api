from __future__ import annotations


def parse_cccd(cccd: str) -> dict:
    """
    Placeholder for Step 4 (parse CCCD).

    Returns a dict shape that later steps can extend.
    """
    return {
        "province_code": None,
        "province_name": None,
        "gender": None,
        "birth_year": None,
    }


